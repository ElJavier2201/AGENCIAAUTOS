# ===========================================
# Configuración de Base de Datos
# Ubicación: src/recursos/db.properties
# ===========================================

# Conexión MySQL
db.url=jdbc:mysql://localhost:3306/agencia_autos
db.driver=com.mysql.cj.jdbc.Driver
db.username=root
db.password=220105

# Pool de Conexiones (HikariCP)
db.pool.maximumPoolSize=10
db.pool.minimumIdle=2
db.pool.connectionTimeout=30000
db.pool.idleTimeout=600000
db.pool.maxLifetime=1800000

# Configuración de caché
db.cachePrepStmts=true
db.prepStmtCacheSize=250
db.prepStmtCacheSqlLimit=2048

# Configuración SSL (para producción)
# db.useSSL=true
# db.requireSSL=true
# db.verifyServerCertificate=true

# ===========================================
# IMPORTANTE:
# - NO subir este archivo a control de versiones (agregar a .gitignore)
# - Usar variables de entorno en producción
# - Cambiar contraseña por defecto
# ===========================================